#include<stdio.h>

int main() {
   char a[100],b[100];
   int i,l=0,l1=0;
   scanf("%s",&a);
   scanf("%s",&b);
   for(i=0;a[i]!='\0';i++)
   {
       printf("%s",a);
       printf("%s",b);
       break;
   }
      return 0;
}
